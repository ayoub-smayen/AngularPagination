import { FilterprodPipe } from './filterprod.pipe';

describe('FilterprodPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterprodPipe();
    expect(pipe).toBeTruthy();
  });
});
